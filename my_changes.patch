diff --git a/.github/workflows/gemini-cli-executor.yml b/.github/workflows/gemini-cli-executor.yml
index ab8960a..222b239 100644
--- a/.github/workflows/gemini-cli-executor.yml
+++ b/.github/workflows/gemini-cli-executor.yml
@@ -7,44 +7,27 @@ on:
     paths:
       - 'triggers/gemini-cli/**'
 
-permissions:
-  contents: write
-
 jobs:
   execute-gemini-cli:
     runs-on: ubuntu-latest
+    timeout-minutes: 30
 
     steps:
       - name: Checkout
-        uses: actions/checkout@v3
+        uses: actions/checkout@v4
 
       - name: Setup Node.js
-        uses: actions/setup-node@v3
+        uses: actions/setup-node@v4
         with:
           node-version: '20'
 
+      - name: Install project dependencies
+        run: npm install
+
       - name: Install Gemini CLI
-        run: |
-          npm install -g @google/generative-ai
+        run: npm install -g @google/generative-ai
 
       - name: Execute with Gemini CLI
         env:
           GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
-        run: |
-          node .github/scripts/gemini-cli-executor.js
-
-      - name: Commit results
-        run: |
-          git config user.name "Gemini CLI Bot"
-          git config user.email "gemini@jamus.dev"
-          git add .
-          git commit -m "✨ Gemini CLI: Task 구현 완료" || echo "No changes"
-          git push origin main
-
-      - name: Notify Slack
-        env:
-          SLACK_WEBHOOK_URL: ${{ secrets.SLACK_WEBHOOK_URL }}
-        run: |
-          curl -X POST "$SLACK_WEBHOOK_URL" \
-            -H "Content-Type: application/json" \
-            -d '{"text":"✅ Gemini CLI 실행 완료!"}'
\ No newline at end of file
+        run: node .github/scripts/gemini-cli-executor.js
\ No newline at end of file
