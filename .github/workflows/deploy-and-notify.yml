name: Deploy and Notify

on:
  deployment_status:

jobs:
  notify-deployment:
    runs-on: ubuntu-latest
    if: github.event.deployment_status.state == 'success'
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v3
        
      - name: Extract Task Number
        id: task
        run: |
          # Extract task number from commit message (first line only)
          COMMIT_MSG=$(git log -1 --pretty=%s)
          echo "Debug: Commit message is '$COMMIT_MSG'"
          TASK_NUM=$(echo "$COMMIT_MSG" | grep -o 'Task [0-9.]\+' | sed 's/Task //' || echo "0")
          echo "Debug: Extracted TASK_NUM is '$TASK_NUM'"
          {
            echo "task_number=$TASK_NUM"
            echo "commit_message<<EOF"
            echo "$COMMIT_MSG"
            echo "EOF"
          } >> $GITHUB_OUTPUT
          
      - name: Get Commit Info
        id: commit
        run: |
          COMMIT_SHA=$(git rev-parse --short HEAD)
          COMMIT_TIME=$(git log -1 --format=%cd --date=format:'%Y-%m-%d %H:%M:%S')
          echo "sha=$COMMIT_SHA" >> $GITHUB_OUTPUT
          echo "time=$COMMIT_TIME" >> $GITHUB_OUTPUT
          
      - name: Send Slack Notification
        env:
          SLACK_WEBHOOK_URL: ${{ secrets.SLACK_WEBHOOK_URL }}
          DEPLOYMENT_URL: ${{ github.event.deployment_status.target_url }}
          TASK_NUMBER: ${{ steps.task.outputs.task_number }}
          COMMIT_SHA: ${{ steps.commit.outputs.sha }}
          COMMIT_TIME: ${{ steps.commit.outputs.time }}
          COMMIT_MESSAGE: ${{ steps.task.outputs.commit_message }}
        run: |
          echo "Debug: TASK_NUMBER before Slack notification is '$TASK_NUMBER'" # Diagnostic line
          if [ "$TASK_NUMBER" != "0" ]; then
            TASK_TITLE="Task ${TASK_NUMBER}"
          else
            TASK_TITLE="배포"
          fi
          
          curl -X POST "$SLACK_WEBHOOK_URL" \
            -H 'Content-Type: application/json' \
            -d '{
              "text": "🚀 배포 완료: '"${TASK_TITLE}"'",
              "blocks": [
                {
                  "type": "header",
                  "text": {
                    "type": "plain_text",
                    "text": "🚀 배포 완료: '"${TASK_TITLE}"'",
                    "emoji": true
                  }
                },
                {
                  "type": "divider"
                },
                {
                  "type": "section",
                  "fields": [
                    {
                      "type": "mrkdwn",
                      "text": "📦 *커밋*\n`'"${COMMIT_SHA}"'`"
                    },
                    {
                      "type": "mrkdwn",
                      "text": "⏰ *시각*\n'"${COMMIT_TIME}"'"
                    }
                  ]
                },
                {
                  "type": "section",
                  "fields": [
                    {
                      "type": "mrkdwn",
                      "text": "🌐 *URL*\n<'"${DEPLOYMENT_URL}"'|배포 확인>"
                    },
                    {
                      "type": "mrkdwn",
                      "text": "💬 *메시지*\n'"${COMMIT_MESSAGE}"'"
                    }
                  ]
                },
                {
                  "type": "divider"
                },
                {
                  "type": "context",
                  "elements": [
                    {
                      "type": "mrkdwn",
                      "text": "✅ Task 완료 시 👍 를 눌러주세요"
                    }
                  ]
                }
              ]
            }'